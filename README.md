# Guestara Booking Calendar

An interactive hotel occupancy heatmap calendar built with React + Vite.

## Setup

npm install
npm run dev

## Stack

- React + Vite
- Tailwind CSS
- No calendar libraries, no date libraries, no drag libraries
